[package]
name = "blake2s"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
honggfuzz = "*"
acvm_blackbox_solver = { path = "../../../acvm/blackbox_solver" }
